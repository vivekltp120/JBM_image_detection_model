
Install dependencies:
  sudo apt install python3-pip
  pip3 install -r requirement.txt

For detect the image from the model:
Run the command:
 python3 load_model_and_test.py 'path for the image folder'



For Train the Model:
Run the command:
 python3 train_classifier.py


Put all the images in the Data folder if you want to trained the model for more data with other parameters.


